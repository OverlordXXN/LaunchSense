"""
Inference interfaces for the trained models.
"""

def predict_success_probability(project_features: dict) -> float:
    """
    Loads trained model and predicts the success probability for a project.
    Returns float between [0, 1].
    """
    return 0.0
